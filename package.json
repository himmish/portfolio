{
  "private": true,
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "next build && next export && echo out/.nojekyll && git add out/ && git commit -m \"Deploy Next.js to gh-pages\" && git push origin --delete gh-pages && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "next": "latest",
    "postcss-cli": "^10.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "autoprefixer": "^10.4.12",
    "postcss": "^8.4.17",
    "tailwindcss": "^3.1.8"
  },
  "devDependencies": {
    "babel-plugin-transform-define": "^1.3.0"
  }
}
